app = "adverb-decision-engine"
primary_region = "iad"

[build]
  dockerfile = "../infra/Dockerfile"

[[services]]
  internal_port = 8080
  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]
